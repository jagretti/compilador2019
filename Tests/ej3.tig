(
let
	type tree = {key: int, children: treelist}
	type treelist = {hd: tree, tl: treelist}
	/* isBin */
	function isBin(t: tree) : int =
		 if t = nil then 1
		      	    else if length(t.children) <= 2
			    	 then 1*isBin
	/* isBComplete */
	/* printInOrder */
	/* printPosOrder */
in
	nil
end
;0)